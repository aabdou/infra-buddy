[project]
name = "agent-raw-tools"
version = "0.1.0"
description = "RAG agent on the Anthropic Messages API, retrieval exposed as a tool"
requires-python = ">=3.13"
dependencies = [
    "infra-core",
    "anthropic>=0.40.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
