# Virtual workspace root: not an installable package itself, it only ties the
# members together so they share one lockfile and one venv.

[tool.uv.workspace]
members = ["packages/*"]

[tool.uv.sources]
infra-core = { workspace = true }
